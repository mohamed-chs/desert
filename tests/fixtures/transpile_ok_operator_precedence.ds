struct Counter:
    value: i32

def main():
    mut counter = Counter(1)
    counter.value = counter.value - (3 - 1)
    let mixed = 1 + 2 * 3
    let grouped = (1 + 2) * 3
    $print("vals {counter.value} {mixed} {grouped}")
